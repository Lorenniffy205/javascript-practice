import { visitorCount, currentMode, defaultPort } from './core/variables.js';

console.log('JavaScript Practice Starter');
console.log('Run daily practice with: npm run day1');
console.log('Core variables sample:', {
  visitorCount,
  currentMode,
  defaultPort,
});
